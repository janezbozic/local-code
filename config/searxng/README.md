# SearXNG configuration

The pinned project-local SearXNG process reads `settings.yml`, binds only to
`127.0.0.1:8888`, disables its limiter/image proxy, and exposes JSON results only
to the localhost safe gateway. It has no automatic startup or update path.
