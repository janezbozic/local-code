# Document tools

`workflow.py` preserves source originals, creates canonical Markdown and
checksum manifests, exports only from canonical Markdown, and renders derived
artifacts for inspection. It refuses to overwrite canonical or derived files.
See `docs/DOCUMENTS.md` for commands and supported formats.
